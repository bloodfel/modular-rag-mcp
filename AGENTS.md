# 项目级工作约定（AGENTS.md）

本文件约束本仓库内所有 Agent 协作的文档与流程约定。

## 文档分层

| 位置 | 内容 | 规则 |
|---|---|---|
| `DEV_SPEC.md`（根目录） | 原作者的全局架构与排期宪法 | **不修改**，只读参考 |
| `docs/specs/YYYY-MM-DD-<feature>.md` | 子功能设计文档 | 一个子功能一篇，日期前缀 |
| `reports/` | 跑批产物（基准测试、评估报告） | 只放输出，不放设计 |

## 子功能 Spec 规范（对齐 DEV_SPEC 第 6 章风格）

每篇 spec 必须包含：

1. **背景与目标 / 非目标**：非目标清单防止范围蔓延；
2. **阶段总览**：接续 DEV_SPEC 已用字母编号（A–I、J 已用，当前从 **K** 起），
   阶段名 + 一句话目的；
3. **📊 进度跟踪表 (Progress Tracking)**：
   - 状态三态：`[ ]` 未开始 | `[~]` 进行中 | `[x]` 已完成
   - 列：任务编号 / 任务名称 / 状态 / 完成日期 / 备注
   - **每完成一个子任务立即更新状态与日期**
4. **验收标准**：可勾选清单，含"测试无新增失败"兜底项。

骨架模板见 `docs/specs/TEMPLATE.md`。

## 子功能实施流程

brainstorming 对话收敛 → 写 spec（本规范格式，用户评审）→ writing-plans 拆任务
→ 逐票实现（TDD，小步提交）→ code-reviewer 审查 → 更新进度跟踪表。

## 代码风格

- 写代码前先跑 `/ponytail`（lazy senior 原则）：最短可用 diff、YAGNI、复用优先于新建、标准库优先；非平凡逻辑留一个最小自检
